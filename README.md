# Choklog - Estoque (deploy)

Build estático, só do módulo Estoque do painel Choklog. Gerado automaticamente a
partir de `generate_deploy_estoque.mjs` no projeto principal
(`C:\Users\andrey.oliveira\Desktop\Comercial\MCP\Logistica`) -- não editar os
arquivos aqui diretamente, eles são sobrescritos a cada publicação.

Publicado via GitHub Pages. Os dados são republicados periodicamente; o
`index.html` faz uma checagem silenciosa a cada 5 minutos e recarrega sozinho
se uma versão mais nova foi publicada, sem mostrar nenhum aviso de atualização.
